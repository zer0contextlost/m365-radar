baseURL = "https://m365radar.zer0contextlost.net/"
languageCode = "en-us"
title = "M365 Radar"

[params]
  description = "Microsoft 365 roadmap changes, translated into a read-only PowerShell check for your own tenant."
  tagline = "Know if it hits your tenant before it ships."

[taxonomies]
  tag = "tags"

[markup.goldmark.renderer]
  unsafe = true
